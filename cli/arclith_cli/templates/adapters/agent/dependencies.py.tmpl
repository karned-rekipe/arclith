"""Process dependencies are assembled by graph.register_agent using its Arclith.

Add a typed immutable dependency container here when nodes need use cases.
Avoid module-level clients, cached service factories or a second Arclith.
"""
