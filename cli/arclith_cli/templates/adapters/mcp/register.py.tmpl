"""FastMCP composition entrypoint generated for this service."""

from fastmcp import FastMCP

from $adapter_import.bindings_generated import (
    register as register_bindings,
)


def register_components(server: FastMCP, use_cases: object) -> None:
    """Register only use cases explicitly exposed through FastMCP."""
    register_bindings(server, use_cases)
